const WindowProperties = require("./window");
const RenderingProperties = require("./rendering");
const DebuggerProperties = require("./debugger");

module.exports = { WindowProperties, RenderingProperties, DebuggerProperties };